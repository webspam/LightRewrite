---
description: Important information for editing Witcher 3 (.ws) files - modding & language conventions
globs: "**/*.ws"
alwaysApply: false
---

# Witcher Script (The Witcher 3)

## Language

- Witcher Script is Red Engine script, **syntax-close to ActionScript 3**, not TypeScript, Java, or C#.
- Do **not** assume npm types, a TS compiler, or C++ builds for `.ws` changes.
- Prefer patterns from **nearby vanilla or mod code** over guessed engine APIs.
- `name` type uses single quotes `'`
  - A `name` becomes a symbol in backend code.
  - `const` vars starting with `TAG_` should always be `name`
  - For convenience, `name` may be implicit cast to `string`, but there is no way to cast variables to `name`
- `string` type uses double quotes `"`
- `int` modulo uses %: `6 % 5 == 1` is `true`, `float` modulo uses `ModF(a, b)`
- `NULL` is idiomatic null (it should be used where it can, but it is a `void` const)

## Layout in this repo

- Mod scripts: `mods/modAbsoluteCamera/content/scripts/` (`.ws`).
- Patched game scripts mirror vanilla paths under `content/scripts/game/...`.

## Style (match this project)

- Use explicit types on members and functions (`: float`, `: string`, `: bool`, `: name`, etc.).
- Initialize fields with `default` on its own line:

```ws
private var isEnabled: bool;  default isEnabled = true;
```

- Defaults must be compile-time const
- Use `enum`, `class`, `function`, and `switch` in the same style as surrounding files.
- Use `LogChannel` or project helpers (e.g. `LogLightRewrite`) consistently with existing files.

## Modding discipline

- Keep diffs **small** on huge patched files (e.g. `r4Player.ws`).
- Preserve comments and call order unless behavior must change.
- Avoid renaming symbols that may be referenced from XML, other scripts, or tooling.

## Witcher 3 script library / Core scripts

- Run .\Import-DotEnv.ps1 to populate `$env:WITCHER_SCRIPTS_PATH`
- The actual game scripts are available in the following locations

| location                            | notes                                        |
| ----------------------------------- | -------------------------------------------- |
| `$env:WITCHER_SCRIPTS_PATH/core/`   | Math, array, string, etc. type decalarations |
| `$env:WITCHER_SCRIPTS_PATH/engine/` | Camera, 3d engine, some base classes         |
| `$env:WITCHER_SCRIPTS_PATH/game/`   | Majority of game code                        |

- Many core functions are imported from native code (fast)

## Witcher 3 DeepWiki

Contains a DeepWiki generated from the complete set of Witcher 3 scripts.

https://deepwiki.com/juanchoix/W3-Scripts/1-overview

## Verification

- There is **no in-repo compiler** here; verification requires human intervention.

## Avoid TS-style shortcuts

```ws
// BAD (TypeScript optional chaining — not Witcher Script)
// foo?.bar

// GOOD (explicit guard)
if (foo) foo.bar;
```

## Common pitfalls

- Ternaries are **not** supported; use e.g.:
  ```ws
  if (thing != otherThing) return true;
  else return false;
  ```
- `RoundF()` behaviour is deterministic but illogical: `RoundF(0.05 * 100.0) / 100.0 == 0.04`
  - `FloorF()` and `CeilF()` work as expected
